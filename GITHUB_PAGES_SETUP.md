# 🚀 GitHub Pages 快速設置指南

## 📋 檢查清單

在開始之前，確保你已經：
- ✅ 擁有 GitHub 帳號
- ✅ 安裝了 Git
- ✅ 項目已經構建成功（`npm run build`）

## 🔧 設置步驟（5分鐘完成）

### 步驟 1：創建 GitHub 倉庫

1. 登入 GitHub
2. 點擊右上角的 `+` → `New repository`
3. 填寫資訊：
   - **Repository name**: `SmartKing`（必須與 vite.config.js 中的 base 一致）
   - **Description**: 智慧王題庫遊戲
   - **Public** 或 **Private** 都可以
   - 不要勾選 "Initialize this repository with a README"
4. 點擊 **Create repository**

### 步驟 2：推送代碼到 GitHub

在項目目錄執行以下命令：

```bash
# 初始化 Git（如果還沒有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 智慧王題庫遊戲"

# 添加遠程倉庫（替換成你的 GitHub 用戶名）
git remote add origin https://github.com/你的用戶名/SmartKing.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步驟 3：啟用 GitHub Pages

1. 進入你的 GitHub 倉庫頁面
2. 點擊 **Settings**（設置）標籤
3. 在左側菜單找到並點擊 **Pages**
4. 在 **Build and deployment** 部分：
   - **Source**: 選擇 `GitHub Actions`
   - 📌 注意：不要選擇 "Deploy from a branch"
5. 保存後會自動開始部署

### 步驟 4：等待部署完成

1. 點擊倉庫頂部的 **Actions** 標籤
2. 你會看到 "Deploy to GitHub Pages" 工作流正在運行
3. 等待綠色✓出現（通常需要 2-3 分鐘）
4. 部署成功後，會顯示網站 URL

### 步驟 5：訪問你的網站 🎉

你的網站將在以下地址可用：

```
https://你的用戶名.github.io/SmartKing/
```

例如：`https://dingyi.github.io/SmartKing/`

---

## 🔄 後續更新流程

每次想更新網站時：

```bash
# 1. 修改代碼後，添加更改
git add .

# 2. 提交更改
git commit -m "描述你的更改"

# 3. 推送到 GitHub
git push origin main
```

推送後，GitHub Actions 會自動：
1. 構建項目
2. 部署到 GitHub Pages
3. 2-3 分鐘後更新完成

---

## ⚠️ 常見問題

### Q1: 網站顯示 404 錯誤
**解決方案**：
- 確認 Settings → Pages 中 Source 選擇的是 `GitHub Actions`
- 檢查 Actions 頁面是否部署成功
- 等待 5-10 分鐘讓 DNS 更新

### Q2: 樣式或圖片無法加載
**解決方案**：
- 確認 `vite.config.js` 中的 `base` 設置為 `/SmartKing/`
- 如果倉庫名稱不是 `SmartKing`，需要修改 `base` 值

### Q3: 部署失敗
**解決方案**：
1. 查看 Actions 頁面的錯誤日誌
2. 確認 `npm run build` 在本地能成功運行
3. 檢查 `package.json` 中的依賴是否完整

### Q4: 推送被拒絕
**解決方案**：
```bash
# 先拉取遠程更改
git pull origin main --rebase

# 再推送
git push origin main
```

### Q5: 想要使用自定義域名
**解決方案**：
1. 在 Settings → Pages 中設置 Custom domain
2. 在你的域名提供商添加 CNAME 記錄
3. 修改 `vite.config.js`：
   ```javascript
   base: '/'  // 改為根路徑
   ```

---

## 🎯 驗證部署成功

✅ 檢查以下項目：
- [ ] 網站可以訪問（沒有 404）
- [ ] 標題動畫正常顯示
- [ ] 可以點擊類別和題目
- [ ] 圖片正確加載
- [ ] 自定義題庫功能正常
- [ ] 計分板可以打開

---

## 📞 需要幫助？

1. 查看 [GitHub Pages 文檔](https://docs.github.com/pages)
2. 查看 [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)
3. 檢查 Actions 頁面的部署日誌
4. 提交 Issue 到項目倉庫

---

## 🎉 成功！

如果你看到這個頁面，恭喜你成功部署了智慧王遊戲到 GitHub Pages！

現在你可以：
- 📤 分享網站鏈接給朋友
- ✏️ 自定義題庫內容
- 🎮 開始遊戲吧！
