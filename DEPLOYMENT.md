# GitHub Pages 部署指南

## 🚀 自動部署設置

本項目已配置 GitHub Actions 自動部署到 GitHub Pages。

### 📋 前置要求

1. 將代碼推送到 GitHub 倉庫
2. 在 GitHub 倉庫中啟用 GitHub Pages

### ⚙️ GitHub Pages 設置步驟

#### 1. 推送代碼到 GitHub

```bash
# 初始化 Git（如果還沒有）
git init

# 添加遠程倉庫
git remote add origin https://github.com/你的用戶名/SmartKing.git

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 推送到 GitHub
git push -u origin main
```

#### 2. 啟用 GitHub Pages

1. 進入你的 GitHub 倉庫
2. 點擊 **Settings**（設置）
3. 在左側菜單找到 **Pages**
4. 在 **Source** 部分：
   - 選擇 **GitHub Actions**（而不是 Deploy from a branch）
5. 保存設置

#### 3. 觸發部署

部署會在以下情況自動觸發：

- ✅ 推送代碼到 `main` 分支
- ✅ Pull Request 合併到 `main` 分支
- ✅ 手動觸發（在 Actions 頁面）

#### 4. 查看部署狀態

1. 進入倉庫的 **Actions** 頁面
2. 查看 "Deploy to GitHub Pages" 工作流
3. 等待構建和部署完成（通常需要 2-3 分鐘）

#### 5. 訪問網站

部署成功後，你的網站將在以下地址可用：

```
https://你的用戶名.github.io/SmartKing/
```

## 🔄 更新網站

每次推送代碼到 `main` 分支時，網站會自動更新：

```bash
# 修改代碼後
git add .
git commit -m "更新內容"
git push origin main
```

## 🛠️ 本地測試構建

在推送前，可以本地測試構建：

```bash
# 構建項目
npm run build

# 預覽構建結果
npm run preview
```

## 📝 注意事項

1. **base 路徑**：`vite.config.js` 已配置為 `/SmartKing/`，這是 GitHub Pages 的子路徑
2. **分支**：確保推送到 `main` 分支（不是 `master`）
3. **權限**：工作流需要 `pages: write` 權限（已在配置中設置）
4. **緩存**：如果更改沒有顯示，嘗試清除瀏覽器緩存或使用無痕模式

## 🐛 故障排除

### 部署失敗

1. 檢查 Actions 頁面的錯誤日誌
2. 確保所有依賴都在 `package.json` 中
3. 確保 `npm run build` 在本地可以成功運行

### 404 錯誤

1. 確認 GitHub Pages 設置正確（Source 選擇 GitHub Actions）
2. 檢查 `vite.config.js` 中的 `base` 路徑是否與倉庫名稱匹配
3. 等待幾分鐘讓 DNS 傳播

### 樣式或資源加載失敗

1. 確認 `base` 路徑配置正確
2. 檢查控制台是否有 CORS 錯誤
3. 確保所有資源路徑都是相對路徑

## 📦 手動部署（備用方案）

如果自動部署有問題，可以手動部署：

```bash
# 構建
npm run build

# 安裝 gh-pages（如果沒有）
npm install -g gh-pages

# 部署到 gh-pages 分支
gh-pages -d dist
```

## 🎉 完成！

設置完成後，每次推送代碼都會自動部署到 GitHub Pages！
